{
  "name": "book-locker-backend",
  "version": "1.0.0",
  "description": "Smart Community Book Exchange Locker - Backend API & Real-time Server",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node scripts/setup-env.js && npx prisma generate && npx prisma db push --accept-data-loss && node src/index.js",
    "build": "node scripts/setup-env.js && npx prisma generate && npx prisma db push",
    "dev": "node --watch src/index.js",
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:seed": "node src/prisma/seed.js",
    "simulate": "node scripts/simulate_esp32.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "aedes": "^0.51.2",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mqtt": "^5.10.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
